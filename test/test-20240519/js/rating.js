/** You can see at "HOME" screen, how this app
 * should look in the end
 * 
 * אפשר לראות למטה במסך הבית איך האפליקציה
 * הזאת אמורה להיראות בסוף
 */



/** MISSION 01:
 * 
 * Create 4 "const":
 *  - for the span of "value"
 *  - for the 3 buttons("decrease","reset","increase") 
 * 
 * Take the selectors from the lines 35-40 of rating.html
 * 
 * 5 points
 * 
 * צרו 4 
 * const:
 * - עבור
 *   span "value"
 * - עבור 3 כפתורים
 *   ("decrease","reset","increase")
 * 
 * קחו את הסלקטורים מתוך הקובץ
 * rating.html
 * 
 * 5 נקודות
 */


/** MISSION 02: 
 * Create global variable "count" and set it's value to 0
 * 
 * 1 point
 * 
 * צרו משתנה גלובאלי בשם
 * "count"
 * ותשימו בתוכו ערך התחלתי של אפס
 * 
 * נקודה 1
*/



/** MISSION 03: 
 * -- Create function (arrow or regular) showCount()
 *    that 
 *    1. shows the "count" value in the span "value"
 *    2. removes from the span "value" all the color
 *       classes: "green", "white" and "red"
 *    3. checks if the "count" is greater than 0,
 *       and if it is, uses class "green" on the span "value"
 *       to color it "green"
 *    4. otherwise, checks if the "count" is smaller than 0,
 *       and if it is, uses class "red" on the span "value"
 *       to color it "red"
 *    5. otherwise, uses class "white" on the span "value"
 *       to color it "white"
 * 
 *    (6. BONUS 3 points - you could use TERNARY OPERATOR
 *        inside TERNARY OPERATOR for the 3.4.5. parts of the mission)
 * 
 * -- Use this function once immediately to show the initial
 *    value on the beginning
 * 
 * 16 points
 * 
 * צרו פונקציה (רגילה או פונקציית חץ) בשם
 * showCount()
 * ש-
 *    1. מציגה את הערך של המשתנה
 *       count
 *       בתוך
 *       span "value"
 *    2. מסירה מ
 *       span "value"
 *       את כל הקלאסים הצבעוניים
 *       "green", "white", "red"
 *    3. בודקת אם הערך של המשתנה
 *       count
 *       גדול מאפס, ואם כן, צובעת את ה
 *       span "value" 
 *       בירוק בעזרת הקלאס
 *       "green"
 *    4. אם לא, בודקת אם הערך של המשתנה
 *       count
 *       קטן מאפס, ואם כן, צובעת את ה
 *       span "value" 
 *       באדום בעזרת הקלאס
 *       "red"
 *    5. אם לא, צובעת את ה
 *       span "value" 
 *       בלבן בעזרת הקלאס
 *       "white"
 *    (6. בונוס של 3 נקודות - אפשר להשתמש באופרטור
 *       המשולש בתוך האופרטור המשולש עבור הסעיפים 3.4.5
 *       של המשימה 3) 
 * 
 * - השתמשו בפונקציה הזאת מיד אחרי הגדרתה
 *   כדי להציג את הערך ההתחלתי של המשתנה
 *   count
 *   ממש על ההתחלה
 * 
 * 16 נקודות
*/




/** MISSION 04: 
 * 
 * Add listener for each button (3 listeners in total)
 * on the event "click" and create anonimous
 * (regular or arrow) callback function
 * for each.
 * 
 * 1. For "decrease" button - function
 *    that subtracts 1 from "count" and uses showCount()
 * 2. For "increase" button - function
 *    that adds 1 to "count" and uses showCount()
 * 3. For "reset" button - function
 *    that sets "count" to be 0 and uses showCount()
 *
 * Ensure that everything works.
 * 
 * 13 points 
 * 
 * הוסיפו ליסטנר עבור כל כפתור (בסך הכל 3 ליסטנרים) על האירוע
 * "click"
 * וצרו פונקציה אנונימית (רגילה או חץ) בתור
 * callback function
 * עבור כל אחד מהליסטנרים.
 * 
 * 1. עבור הכפתור
 *    "decrease" -
 *    פונקציה שמחסירה 1 מהמשתנה
 *    "count"
 *    וקוראת לפונקציה
 *    showCount()
 * 2. עבור הכפתור
 *    "increase" -
 *    פונקציה שמוסיפה 1 למשתנה
 *    "count"
 *    וקוראת לפונקציה
 *    showCount()
 * 3. עבור הכפתור
 *    "reset" -
 *    פונקציה ששמה 0 בתוך המשתנה
 *    "count"
 *    וקוראת לפונקציה
 *    showCount()
 * 
 * ודאו שהכל עובד.
 * 
 * 13 נקודות
 * 
 */


